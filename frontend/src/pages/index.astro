---
// Landing page - AGENTES by Luminari — Compact redesign
---
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>AGENTES - IA que trabaja por tu negocio</title>
  <meta name="description" content="Automatiza tareas, atiende clientes y ejecuta procesos directamente desde WhatsApp. Sin fricción, sin demoras, sin errores." />
  <meta property="og:title" content="AGENTES - IA que trabaja por tu negocio" />
  <meta property="og:description" content="Un empleado AI que hace cosas reales por tu negocio. Automatiza tareas desde WhatsApp." />
  <meta property="og:image" content="/og.png" />
  <meta property="og:type" content="website" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet" />
  <script defer src="https://umami.luminari.agency/script.js" data-website-id="9a5bb1d1-9bc1-417a-8b4b-72b44044d168"></script>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    :root {
      --green: #25D366;
      --green-dark: #128C7E;
      --green-light: #e8faf0;
      --white: #ffffff;
      --gray-50: #f9fafb;
      --gray-100: #f3f4f6;
      --gray-200: #e5e7eb;
      --gray-300: #d1d5db;
      --gray-400: #9ca3af;
      --gray-500: #6b7280;
      --gray-600: #4b5563;
      --gray-700: #374151;
      --gray-800: #1f2937;
      --gray-900: #111827;
    }
    html { scroll-behavior: smooth; }
    body {
      font-family: 'Inter', -apple-system, sans-serif;
      background: var(--white);
      color: var(--gray-800);
      line-height: 1.6;
      -webkit-font-smoothing: antialiased;
    }
    .container { max-width: 1200px; margin: 0 auto; padding: 0 24px; }
    .mono-label {
      font-size: 10px; font-weight: 700; text-transform: uppercase;
      letter-spacing: 0.12em; color: var(--gray-400);
    }

    /* REVEAL */
    .reveal { opacity: 1; transform: translateY(0); }
    .reveal-ready .reveal { opacity: 0; transform: translateY(24px); transition: opacity 0.6s ease, transform 0.6s ease; }
    .reveal-ready .reveal.visible { opacity: 1; transform: translateY(0); }
    @media (prefers-reduced-motion: reduce) {
      .reveal-ready .reveal { opacity: 1; transform: none; transition: none; }
    }

    /* NAV */
    nav {
      position: fixed; top: 0; left: 0; right: 0; z-index: 100;
      background: rgba(255,255,255,0.9); backdrop-filter: blur(20px);
      border-bottom: 1px solid var(--gray-100);
      transition: all 0.3s;
    }
    nav.scrolled { background: rgba(255,255,255,0.95); box-shadow: 0 1px 8px rgba(0,0,0,0.04); }
    nav .inner {
      max-width: 1200px; margin: 0 auto; padding: 0 24px;
      display: flex; align-items: center; justify-content: space-between;
      height: 56px;
    }
    .logo { text-decoration: none; font-weight: 800; font-size: 1.1rem; letter-spacing: .12em; color: var(--gray-900); text-transform: uppercase; }
    .logo .diamond { color: var(--green); }
    nav .links { display: flex; align-items: center; gap: 28px; }
    nav .links a {
      text-decoration: none; font-size: 11px; font-weight: 600;
      text-transform: uppercase; letter-spacing: 0.08em;
      color: var(--gray-500); transition: color 0.2s;
    }
    nav .links a:hover { color: var(--green); }
    .nav-cta {
      background: var(--green) !important; color: white !important;
      padding: 7px 20px; border-radius: 8px;
      box-shadow: 0 4px 16px rgba(37,211,102,0.2);
      transition: all 0.2s;
    }
    .nav-cta:hover { transform: translateY(-1px); box-shadow: 0 6px 20px rgba(37,211,102,0.3); }

    /* HERO */
    .hero {
      padding: 120px 0 0;
      text-align: center;
      position: relative;
    }
    .hero-badge {
      display: inline-flex; align-items: center; gap: 8px;
      background: rgba(37,211,102,0.06); border: 1px solid rgba(37,211,102,0.15);
      padding: 4px 14px; border-radius: 6px;
      font-size: 10px; text-transform: uppercase; font-weight: 700;
      letter-spacing: 0.12em; color: var(--green);
      margin-bottom: 24px;
    }
    .hero-badge .dot {
      position: relative; width: 6px; height: 6px;
    }
    .hero-badge .dot::before {
      content: ''; position: absolute; inset: 0;
      border-radius: 50%; background: var(--green);
      animation: ping 2s infinite;
    }
    .hero-badge .dot::after {
      content: ''; position: absolute; inset: 0;
      border-radius: 50%; background: var(--green);
    }
    @keyframes ping { 0% { transform: scale(1); opacity: 1; } 75%,100% { transform: scale(2.5); opacity: 0; } }
    .hero h1 {
      font-size: clamp(2.5rem, 5.5vw, 4.5rem);
      font-weight: 900; line-height: 0.92; letter-spacing: -0.04em;
      color: var(--gray-900); margin-bottom: 20px;
    }
    .hero h1 em { font-style: normal; color: var(--green); }
    .hero p {
      font-size: clamp(0.95rem, 1.8vw, 1.15rem);
      color: var(--gray-500); max-width: 540px; margin: 0 auto 32px;
      line-height: 1.6; font-weight: 500;
    }
    .hero-cta {
      display: inline-flex; align-items: center; gap: 10px;
      background: var(--green); color: white; padding: 16px 40px;
      border-radius: 8px; font-weight: 700; font-size: 13px;
      text-transform: uppercase; letter-spacing: 0.1em;
      text-decoration: none; transition: all 0.3s;
      box-shadow: 0 8px 32px rgba(37,211,102,0.25);
    }
    .hero-cta:hover { transform: translateY(-2px); box-shadow: 0 12px 40px rgba(37,211,102,0.35); }
    .hero-cta svg { width: 16px; height: 16px; }

    /* STATS BAR */
    .stats-bar {
      margin-top: 48px;
      padding: 20px 0;
      background: var(--gray-50);
      border-top: 1px solid var(--gray-100);
      border-bottom: 1px solid var(--gray-100);
    }
    .stats-bar .inner {
      display: flex; align-items: center; justify-content: center;
      gap: 40px; flex-wrap: wrap;
    }
    .stat-item {
      display: flex; align-items: center; gap: 8px;
      font-size: 11px; font-weight: 700; text-transform: uppercase;
      letter-spacing: 0.08em; color: var(--gray-500);
    }
    .stat-item svg { width: 14px; height: 14px; color: var(--green); }

    /* PAIN POINTS */
    .pain-points { padding: 32px 0; background: #f0faf4; border-bottom: 1px solid var(--gray-100); }
    .pain-grid {
      display: grid; grid-template-columns: repeat(4, 1fr); gap: 16px;
    }
    .pain-item {
      display: flex; align-items: center; gap: 10px;
      font-size: 13px; font-weight: 500; color: var(--gray-600); line-height: 1.4;
    }
    .pain-item svg { width: 18px; height: 18px; flex-shrink: 0; color: var(--gray-400); }
    @media (max-width: 768px) { .pain-grid { grid-template-columns: 1fr 1fr; } }
    @media (max-width: 480px) { .pain-grid { grid-template-columns: 1fr; } }

    /* CAPABILITIES GRID */
    .capabilities { padding: 64px 0; border-bottom: 1px solid var(--gray-100); }
    .section-header { text-align: center; margin-bottom: 40px; }
    .section-header h2 {
      font-size: clamp(1.5rem, 3vw, 2.2rem);
      font-weight: 900; color: var(--gray-900); letter-spacing: -0.03em;
      text-transform: uppercase;
    }
    .cap-grid {
      display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px;
    }
    .cap-card {
      padding: 24px; border-radius: 12px;
      border: 1px solid var(--gray-200); background: white;
      transition: all 0.3s;
    }
    .cap-card:hover { box-shadow: 0 6px 24px rgba(0,0,0,0.06); border-color: rgba(37,211,102,0.3); }
    .cap-card .cap-icon {
      width: 36px; height: 36px; background: var(--gray-50);
      border: 1px solid var(--gray-200); border-radius: 8px;
      display: flex; align-items: center; justify-content: center;
      margin-bottom: 14px; color: var(--green); transition: all 0.3s;
    }
    .cap-card:hover .cap-icon { background: var(--green); color: white; }
    .cap-card .cap-icon svg { width: 18px; height: 18px; }
    .cap-card h3 {
      font-size: 13px; font-weight: 700; text-transform: uppercase;
      letter-spacing: -0.01em; color: var(--gray-900); margin-bottom: 6px;
    }
    .cap-card p { font-size: 12px; color: var(--gray-500); line-height: 1.5; }

    /* PRICING */
    .pricing { padding: 64px 0; background: var(--gray-50); border-bottom: 1px solid var(--gray-100); }
    .pricing-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 24px; max-width: 720px; margin: 0 auto; }
    .pricing-card {
      padding: 32px; border-radius: 20px; display: flex; flex-direction: column;
      border: 1px solid var(--gray-200); background: white;
      transition: all 0.3s;
    }
    .pricing-card:hover { box-shadow: 0 12px 48px rgba(0,0,0,0.08); }
    .pricing-card.featured {
      background: var(--green); color: white; border-color: var(--green);
      box-shadow: 0 12px 48px rgba(37,211,102,0.2);
      position: relative; overflow: hidden;
    }
    .pricing-badge-ent {
      position: absolute; top: 24px; right: 24px;
      background: rgba(255,255,255,0.2); color: white;
      font-size: 8px; font-weight: 700; text-transform: uppercase;
      letter-spacing: 0.12em; padding: 3px 10px; border-radius: 999px;
      backdrop-filter: blur(8px);
    }
    .pricing-card h3 {
      font-size: 1.3rem; font-weight: 900; text-transform: uppercase;
      letter-spacing: -0.01em; margin-bottom: 4px;
    }
    .pricing-card .pricing-desc {
      font-size: 13px; margin-bottom: 20px; line-height: 1.5;
    }
    .pricing-card:not(.featured) .pricing-desc { color: var(--gray-400); }
    .featured .pricing-desc { color: rgba(255,255,255,0.8); }
    .price-amount { font-size: 2.5rem; font-weight: 900; letter-spacing: -0.03em; }
    .price-period { font-size: 13px; font-weight: 700; margin-left: 4px; }
    .pricing-card:not(.featured) .price-period { color: var(--gray-400); }
    .featured .price-period { color: rgba(255,255,255,0.6); }
    .price-soles { margin-top: 2px; margin-bottom: 20px; }
    .pricing-features { list-style: none; display: flex; flex-direction: column; gap: 10px; margin-bottom: 24px; flex: 1; }
    .pricing-features li {
      display: flex; align-items: center; gap: 10px;
      font-size: 13px; font-weight: 500;
    }
    .pricing-features li svg { width: 14px; height: 14px; flex-shrink: 0; }
    .pricing-card:not(.featured) .pricing-features li { color: var(--gray-600); }
    .pricing-card:not(.featured) .pricing-features li svg { color: var(--green); }
    .featured .pricing-features li { color: rgba(255,255,255,0.9); }
    .featured .pricing-features li svg { color: white; }
    .pricing-btn {
      display: block; width: 100%; text-align: center; padding: 14px;
      border-radius: 8px; font-weight: 700; font-size: 12px;
      text-transform: uppercase; letter-spacing: 0.1em;
      text-decoration: none; transition: all 0.2s; border: none; cursor: pointer;
    }
    .pricing-btn.dark { background: var(--gray-900); color: white; }
    .pricing-btn.dark:hover { background: black; }
    .pricing-btn.white { background: white; color: var(--green); box-shadow: 0 8px 24px rgba(0,0,0,0.1); }
    .pricing-btn.white:hover { background: var(--gray-50); }

    /* FORM */
    .form-section { padding: 64px 0; border-bottom: 1px solid var(--gray-100); }
    .form-inner { max-width: 520px; margin: 0 auto; }
    .form-wrapper {
      background: white; padding: 32px; border-radius: 20px;
      border: 1px solid var(--gray-200);
      box-shadow: 0 12px 48px rgba(0,0,0,0.06);
    }
    .progress-bar { display: flex; gap: 6px; margin-bottom: 24px; }
    .progress-bar .bar {
      flex: 1; height: 3px; border-radius: 999px;
      background: var(--gray-200); transition: background 0.4s;
    }
    .progress-bar .bar.active { background: var(--green); }
    .form-step { display: none; animation: fadeIn 0.4s ease; }
    .form-step.active { display: block; }
    @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
    .form-step h3 { font-size: 1.1rem; font-weight: 800; color: var(--gray-900); margin-bottom: 6px; }
    .form-step .hint { font-size: 13px; color: var(--gray-500); margin-bottom: 20px; }
    label.field { display: block; margin-bottom: 14px; }
    label.field span { display: block; font-size: 10px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.1em; color: var(--gray-400); margin-bottom: 5px; margin-left: 4px; }
    input[type="text"], input[type="email"], input[type="tel"] {
      width: 100%; padding: 14px; border: 1.5px solid var(--gray-100);
      border-radius: 10px; font-size: 14px; font-family: inherit;
      background: var(--gray-50); color: var(--gray-900);
      transition: all 0.2s;
    }
    input:focus { outline: none; border-color: var(--green); background: white; box-shadow: 0 0 0 3px rgba(37,211,102,0.12); }
    .checkbox-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; }
    .checkbox-item {
      display: flex; align-items: center; gap: 8px;
      padding: 10px 12px; border: 1.5px solid var(--gray-200);
      border-radius: 10px; cursor: pointer; transition: all 0.2s;
      font-size: 13px; font-weight: 500; color: var(--gray-700);
    }
    .checkbox-item:hover { border-color: var(--green); }
    .checkbox-item.checked { border-color: var(--green); background: var(--green-light); color: #15803d; }
    .checkbox-item input { display: none; }
    .form-nav { display: flex; gap: 10px; margin-top: 20px; }
    .btn-back {
      flex: 1; padding: 14px; border-radius: 10px;
      border: 1.5px solid var(--gray-200); background: white;
      font-family: inherit; font-weight: 600; font-size: 13px;
      color: var(--gray-600); cursor: pointer; transition: all 0.2s;
    }
    .btn-back:hover { border-color: var(--gray-400); }
    .btn-next, .btn-submit {
      flex: 2; padding: 14px; border-radius: 10px;
      border: none; background: var(--green);
      font-family: inherit; font-weight: 700; font-size: 13px;
      color: white; cursor: pointer; transition: all 0.2s;
    }
    .btn-next:hover, .btn-submit:hover { box-shadow: 0 4px 16px rgba(37,211,102,0.35); transform: translateY(-1px); }
    .btn-next:disabled, .btn-submit:disabled { opacity: 0.5; cursor: not-allowed; transform: none; box-shadow: none; }
    .success-msg { display: none; text-align: center; padding: 16px; }
    .success-msg.show { display: block; animation: fadeIn 0.5s ease; }
    .success-msg h3 { font-size: 1.2rem; font-weight: 800; color: var(--gray-900); margin-bottom: 6px; }
    .success-msg p { color: var(--gray-500); font-size: 13px; }

    /* FAQ */
    .faq { padding: 64px 0; }
    .faq-list { max-width: 700px; margin: 0 auto; background: white; border: 1px solid var(--gray-200); border-radius: 16px; padding: 16px 24px; }
    .faq-item { border-bottom: 1px solid var(--gray-100); padding: 14px 0; }
    .faq-item:last-child { border-bottom: none; }
    .faq-question {
      display: flex; align-items: center; justify-content: space-between;
      width: 100%; padding: 0; cursor: pointer; background: none; border: none;
      font-family: inherit; text-align: left;
      font-size: 13px; font-weight: 700; text-transform: uppercase;
      letter-spacing: -0.01em; color: var(--gray-900); transition: color 0.2s;
    }
    .faq-question:hover { color: var(--green); }
    .faq-toggle {
      width: 22px; height: 22px; border-radius: 50%;
      background: var(--gray-50); display: flex; align-items: center; justify-content: center;
      transition: all 0.3s; flex-shrink: 0;
    }
    .faq-toggle svg { width: 12px; height: 12px; color: var(--gray-400); transition: transform 0.3s; }
    .faq-item.open .faq-toggle { background: var(--green); }
    .faq-item.open .faq-toggle svg { color: white; transform: rotate(180deg); }
    .faq-answer { max-height: 0; overflow: hidden; transition: max-height 0.4s ease; }
    .faq-item.open .faq-answer { max-height: 200px; }
    .faq-answer p { font-size: 13px; color: var(--gray-500); line-height: 1.6; padding-top: 12px; max-width: 550px; }

    /* FOOTER */
    footer {
      padding: 40px 0; border-top: 1px solid var(--gray-100); background: var(--gray-50);
    }
    footer .inner {
      max-width: 1200px; margin: 0 auto; padding: 0 24px;
      display: flex; justify-content: space-between; align-items: center;
    }
    footer .brand { display: flex; flex-direction: column; align-items: flex-start; gap: 12px; }
    footer .brand-logo { display: flex; align-items: center; gap: 8px; }
    footer .brand-logo .li { width: 22px; height: 22px; background: var(--green); border-radius: 4px; display: flex; align-items: center; justify-content: center; }
    footer .brand-logo .li svg { width: 11px; height: 11px; color: white; }
    footer .brand-logo span { font-weight: 900; font-size: 1rem; text-transform: uppercase; letter-spacing: -0.03em; }
    footer .foot-links { display: flex; gap: 32px; }
    footer .foot-links a { font-size: 10px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.1em; color: var(--gray-400); text-decoration: none; transition: color 0.2s; }
    footer .foot-links a:hover { color: var(--green); }

    /* MOBILE */
    @media (max-width: 768px) {
      .cap-grid { grid-template-columns: 1fr 1fr; }
      .pricing-grid { grid-template-columns: 1fr; max-width: 360px; }
      .checkbox-grid { grid-template-columns: 1fr; }
      .stats-bar .inner { gap: 20px; }
      .stat-item { font-size: 10px; }
      nav .links a:not(.nav-cta) { display: none; }
      footer .inner { flex-direction: column; gap: 24px; text-align: center; }
      footer .brand { align-items: center; }
    }
    @media (max-width: 480px) {
      .cap-grid { grid-template-columns: 1fr; }
    }
  </style>
</head>
<body>

<!-- NAV -->
<nav id="main-nav">
  <div class="inner">
    <a href="/" class="logo"><span class="diamond">&#9670;</span> AGENTES</a>
    <div class="links">
      <a href="#precios">Precios</a>
      <a href="#empezar" class="nav-cta">Empezar</a>
    </div>
  </div>
</nav>

<!-- HERO -->
<section class="hero">
  <div class="container">
    <div class="hero-badge">
      <div class="dot"></div>
      Disponible ahora
    </div>
    <h1>IA QUE TRABAJA<br><em>POR TU NEGOCIO</em></h1>
    <p>¿Cansado de responder los mismos mensajes, perder clientes por no contestar a tiempo y hacer todo tú solo? Tu agente se encarga mientras tú te enfocas en crecer.</p>
    <a href="#empezar" class="hero-cta">
      Quiero mi agente
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M7 17 17 7"/><path d="M7 7h10v10"/></svg>
    </a>
  </div>
</section>

<!-- STATS BAR -->
<div class="stats-bar">
  <div class="container">
    <div class="inner">
      <div class="stat-item">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
        Disponible 24/7
      </div>
      <div class="stat-item">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/><path d="m9 12 2 2 4-4"/></svg>
        Soporte incluido
      </div>
      <div class="stat-item">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg>
        Desde $300/mes
      </div>
      <div class="stat-item">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="13 2 3 14h9l-1 8 10-12h-9l1-8z"/></svg>
        Setup en 48h
      </div>
    </div>
  </div>
</div>

<!-- PAIN POINTS -->
<section class="pain-points">
  <div class="container">
    <div class="pain-grid">
      <div class="pain-item">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="15" y1="9" x2="9" y2="15"/><line x1="9" y1="9" x2="15" y2="15"/></svg>
        Pierdes clientes por no responder a tiempo
      </div>
      <div class="pain-item">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
        Gastas horas en tareas que no generan ingresos
      </div>
      <div class="pain-item">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 0 1-3.46 0"/></svg>
        Tu negocio se detiene cuando tú no estás
      </div>
      <div class="pain-item">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg>
        Contratar personal es caro y complicado
      </div>
    </div>
  </div>
</section>

<!-- QUE HACE TU AGENTE -->
<section class="capabilities">
  <div class="container">
    <div class="section-header reveal">
      <h2>Que hace tu agente</h2>
    </div>
    <div class="cap-grid reveal">
      <div class="cap-card">
        <div class="cap-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
        </div>
        <h3>Atención al cliente</h3>
        <p>Soporte automatizado por WhatsApp, tickets y cierre de ventas asistido.</p>
        <p style="font-size:11px;color:var(--green-dark);margin-top:6px;font-weight:500">Nunca más pierdas un cliente por no contestar a las 11pm.</p>
      </div>
      <div class="cap-card">
        <div class="cap-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="17 1 21 5 17 9"/><path d="M3 11V9a4 4 0 0 1 4-4h14"/><polyline points="7 23 3 19 7 15"/><path d="M21 13v2a4 4 0 0 1-4 4H3"/></svg>
        </div>
        <h3>Automatización</h3>
        <p>Sincronización de datos, reportes automáticos y facturación recurrente.</p>
        <p style="font-size:11px;color:var(--green-dark);margin-top:6px;font-weight:500">Deja de hacer manualmente lo que una IA puede resolver en segundos.</p>
      </div>
      <div class="cap-card">
        <div class="cap-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18"/><path d="M9 21V9"/></svg>
        </div>
        <h3>Desarrollo web</h3>
        <p>Landing pages, sistemas internos y mantenimiento de infraestructura.</p>
        <p style="font-size:11px;color:var(--green-dark);margin-top:6px;font-weight:500">Tu presencia digital profesional sin depender de un programador.</p>
      </div>
      <div class="cap-card">
        <div class="cap-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="M7 15h0M2 9.5h20"/></svg>
        </div>
        <h3>Contabilidad</h3>
        <p>Control de gastos, conciliación y reportes financieros automatizados.</p>
        <p style="font-size:11px;color:var(--green-dark);margin-top:6px;font-weight:500">Olvídate de las hojas de Excel que nadie actualiza.</p>
      </div>
      <div class="cap-card">
        <div class="cap-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg>
        </div>
        <h3>Ventas</h3>
        <p>Calificación de leads, seguimiento y actualización de embudos CRM.</p>
        <p style="font-size:11px;color:var(--green-dark);margin-top:6px;font-weight:500">Que ningún interesado se te escape por falta de seguimiento.</p>
      </div>
      <div class="cap-card">
        <div class="cap-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="3" width="20" height="14" rx="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/></svg>
        </div>
        <h3>Monitoreo</h3>
        <p>Detección proactiva de fallos y autorecuperación de servicios.</p>
        <p style="font-size:11px;color:var(--green-dark);margin-top:6px;font-weight:500">Tus sistemas funcionando aunque tú estés durmiendo.</p>
      </div>
    </div>
  </div>
</section>

<!-- PRICING -->
<section class="pricing" id="precios">
  <div class="container">
    <div class="section-header reveal">
      <h2>Planes</h2>
      <p style="font-size:14px;color:var(--gray-500);margin-top:10px;font-weight:500">Un empleado tradicional te cuesta más de $1,200/mes. Tu agente digital desde $300.</p>
    </div>
    <div class="pricing-grid reveal">
      <div class="pricing-card">
        <div class="mono-label" style="margin-bottom:12px">Entry Level</div>
        <h3 style="color:var(--gray-900)">Agente Compartido</h3>
        <div class="pricing-desc">Automatización inicial para validar resultados</div>
        <div class="price-amount" style="color:var(--gray-900)">$300<span class="price-period">/MES</span></div>
        <div class="price-soles mono-label">~S/1,140</div>
        <ul class="pricing-features">
          <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>Motor de IA compartido</li>
          <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>Flujos de trabajo estándar</li>
          <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>Integraciones via Zapier/Make</li>
          <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>Soporte técnico prioritario</li>
        </ul>
        <a href="#empezar" class="pricing-btn dark">Seleccionar</a>
      </div>
      <div class="pricing-card featured">
        <div class="pricing-badge-ent">Recomendado</div>
        <div class="mono-label" style="color:rgba(255,255,255,0.6);margin-bottom:12px">High Performance</div>
        <h3>Agente Dedicado</h3>
        <div class="pricing-desc">Infraestructura exclusiva para escalar</div>
        <div class="price-amount">$500<span class="price-period">/MES</span></div>
        <div class="price-soles mono-label" style="color:rgba(255,255,255,0.4)">~S/1,900</div>
        <ul class="pricing-features">
          <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>Instancia de IA dedicada</li>
          <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>Automatizaciones complejas</li>
          <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>Integraciones custom</li>
          <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>SLA de respuesta garantizado</li>
        </ul>
        <a href="#empezar" class="pricing-btn white">Seleccionar</a>
      </div>
    </div>
  </div>
</section>

<!-- FORM -->
<section class="form-section" id="empezar">
  <div class="container">
    <div class="section-header reveal">
      <h2>Solicita tu agente</h2>
    </div>
    <div class="form-inner reveal">
      <div class="form-wrapper">
        <div class="progress-bar">
          <div class="bar active" data-step="1"></div>
          <div class="bar" data-step="2"></div>
          <div class="bar" data-step="3"></div>
        </div>
        <form id="onboarding-form">
          <div class="form-step active" data-step="1">
            <h3>Tu negocio</h3>
            <p class="hint">Tu marca, empresa o proyecto.</p>
            <label class="field">
              <span>Nombre del negocio</span>
              <input type="text" id="business-name" placeholder="Ej: Clínica San Martín" required />
            </label>
            <div class="form-nav">
              <button type="button" class="btn-next" id="btn-step1">Continuar</button>
            </div>
          </div>
          <div class="form-step" data-step="2">
            <h3>Areas de ayuda</h3>
            <p class="hint">Elige todas las que necesites.</p>
            <div class="checkbox-grid">
              <label class="checkbox-item"><input type="checkbox" name="areas" value="ventas" /> Ventas</label>
              <label class="checkbox-item"><input type="checkbox" name="areas" value="soporte" /> Soporte</label>
              <label class="checkbox-item"><input type="checkbox" name="areas" value="contabilidad" /> Contabilidad</label>
              <label class="checkbox-item"><input type="checkbox" name="areas" value="diseño" /> Diseño</label>
              <label class="checkbox-item"><input type="checkbox" name="areas" value="desarrollo-web" /> Desarrollo web</label>
              <label class="checkbox-item"><input type="checkbox" name="areas" value="redes-sociales" /> Redes sociales</label>
              <label class="checkbox-item"><input type="checkbox" name="areas" value="citas" /> Agendar citas</label>
              <label class="checkbox-item"><input type="checkbox" name="areas" value="otro" /> Otro</label>
            </div>
            <div class="form-nav">
              <button type="button" class="btn-back" id="btn-back2">Atras</button>
              <button type="button" class="btn-next" id="btn-step2">Continuar</button>
            </div>
          </div>
          <div class="form-step" data-step="3">
            <h3>Contacto</h3>
            <p class="hint">Te escribiremos para poner tu agente a trabajar.</p>
            <label class="field">
              <span>Correo electrónico</span>
              <input type="email" id="email" placeholder="tu@empresa.com" required />
            </label>
            <label class="field">
              <span>WhatsApp</span>
              <input type="tel" id="whatsapp" placeholder="+51 999 999 999" required />
            </label>
            <div class="form-nav">
              <button type="button" class="btn-back" id="btn-back3">Atras</button>
              <button type="submit" class="btn-submit">Enviar solicitud</button>
            </div>
          </div>
        </form>
        <div class="success-msg" id="success">
          <h3>Listo</h3>
          <p>Recibimos tu solicitud. Te contactaremos pronto.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- FAQ -->
<section class="faq">
  <div class="container">
    <div class="section-header reveal">
      <h2>Preguntas frecuentes</h2>
    </div>
    <div class="faq-list reveal">
      <div class="faq-item">
        <button class="faq-question">¿Funciona para mi tipo de negocio? <span class="faq-toggle"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg></span></button>
        <div class="faq-answer"><p>Si. Trabajamos con clínicas, restaurantes, tiendas, consultoras y más. Si tu negocio atiende clientes o maneja procesos repetitivos, un agente te va a servir.</p></div>
      </div>
      <div class="faq-item">
        <button class="faq-question">¿Mis clientes sabrán que es una IA? <span class="faq-toggle"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg></span></button>
        <div class="faq-answer"><p>El agente responde con el tono y estilo de tu marca. La experiencia es natural y fluida. Si prefieres que se identifique como asistente virtual, también se puede configurar.</p></div>
      </div>
      <div class="faq-item">
        <button class="faq-question">¿Y si necesito algo que el agente no sabe hacer? <span class="faq-toggle"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg></span></button>
        <div class="faq-answer"><p>Lo entrenamos y adaptamos a lo que necesites. Si surge algo nuevo, lo agregamos. Tu agente aprende y mejora con el tiempo.</p></div>
      </div>
      <div class="faq-item">
        <button class="faq-question">¿Puedo probarlo antes de comprometerme? <span class="faq-toggle"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg></span></button>
        <div class="faq-answer"><p>Si. Hacemos una demo personalizada para tu negocio para que veas exactamente cómo funcionaría antes de decidir.</p></div>
      </div>
      <div class="faq-item">
        <button class="faq-question">¿Cómo se conecta a mi WhatsApp? <span class="faq-toggle"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg></span></button>
        <div class="faq-answer"><p>Nosotros nos encargamos de toda la configuración técnica. Solo necesitas darnos acceso y en menos de 48 horas tu agente está operativo.</p></div>
      </div>
      <div class="faq-item">
        <button class="faq-question">¿Qué pasa si quiero cancelar? <span class="faq-toggle"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg></span></button>
        <div class="faq-answer"><p>Sin contratos largos ni penalidades. Puedes cancelar cuando quieras. Creemos que te quedas porque funciona, no porque te obligamos.</p></div>
      </div>
    </div>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <div class="inner">
    <div class="brand">
      <div class="brand-logo">
        <div class="li">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="4" y="4" width="16" height="16" rx="2"/><rect x="9" y="9" width="6" height="6"/></svg>
        </div>
        <span>Agentes</span>
      </div>
      <div class="mono-label">by <a href="https://luminari.agency/" target="_blank" rel="noopener" style="color:var(--green);text-decoration:none">Luminari Agency</a></div>
    </div>
    <div class="foot-links">
      <a href="#">Privacidad</a>
      <a href="#">Términos</a>
    </div>
  </div>
</footer>

<script>
  // Nav scroll
  window.addEventListener('scroll', function() {
    document.getElementById('main-nav').classList.toggle('scrolled', window.scrollY > 20);
  });

  // Form multi-step
  function goToStep(n) {
    document.querySelectorAll('.form-step').forEach(function(s) { s.classList.remove('active'); });
    document.querySelector('.form-step[data-step="' + n + '"]').classList.add('active');
    document.querySelectorAll('.progress-bar .bar').forEach(function(b) {
      b.classList.toggle('active', parseInt(b.dataset.step) <= n);
    });
  }
  document.getElementById('btn-step1').addEventListener('click', function() { goToStep(2); });
  document.getElementById('btn-step2').addEventListener('click', function() { goToStep(3); });
  document.getElementById('btn-back2').addEventListener('click', function() { goToStep(1); });
  document.getElementById('btn-back3').addEventListener('click', function() { goToStep(2); });

  // Checkbox toggle
  document.querySelectorAll('.checkbox-item').forEach(function(item) {
    item.addEventListener('click', function() {
      var cb = item.querySelector('input');
      setTimeout(function() { item.classList.toggle('checked', cb.checked); }, 0);
    });
  });

  // Form submit
  document.getElementById('onboarding-form').addEventListener('submit', async function(e) {
    e.preventDefault();
    var btn = e.target.querySelector('.btn-submit');
    btn.disabled = true;
    btn.textContent = 'Enviando...';
    var areas = Array.from(document.querySelectorAll('input[name="areas"]:checked')).map(function(c) { return c.value; });
    try {
      await fetch('https://agentes-api.luminari.agency/api/waitlist', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          businessName: document.getElementById('business-name').value,
          areas: areas,
          email: document.getElementById('email').value,
          whatsapp: document.getElementById('whatsapp').value,
        })
      });
    } catch (err) {}
    document.getElementById('onboarding-form').style.display = 'none';
    document.querySelector('.progress-bar').style.display = 'none';
    document.getElementById('success').classList.add('show');
  });

  // FAQ accordion
  document.querySelectorAll('.faq-question').forEach(function(btn) {
    btn.addEventListener('click', function() {
      var item = btn.parentElement;
      var wasOpen = item.classList.contains('open');
      document.querySelectorAll('.faq-item').forEach(function(i) { i.classList.remove('open'); });
      if (!wasOpen) item.classList.add('open');
    });
  });

  // Scroll reveal
  (function() {
    var reveals = document.querySelectorAll('.reveal');
    if (!('IntersectionObserver' in window)) {
      reveals.forEach(function(el) { el.classList.add('visible'); });
      return;
    }
    document.documentElement.classList.add('reveal-ready');
    var obs = new IntersectionObserver(function(entries) {
      entries.forEach(function(entry) {
        if (entry.isIntersecting) { entry.target.classList.add('visible'); obs.unobserve(entry.target); }
      });
    }, { threshold: 0.05, rootMargin: '0px 0px -30px 0px' });
    reveals.forEach(function(el) { obs.observe(el); });
    setTimeout(function() { reveals.forEach(function(el) { el.classList.add('visible'); }); }, 2500);
  })();
</script>

</body>
</html>
